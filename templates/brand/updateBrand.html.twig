{% extends "base.html.twig" %}

{% block title %}Update Brand{% endblock %}

{% block body %}
<div class="container-fluid mt-3">
      <h2 class="text-center mt-5">Update Brand</h2>
      <div class="col-md-6"></div>
          {{ form_start (form) }}
              <div class="form-group">
                  {{ form_label (form.brandName) }}
                  {{ form_widget (form.brandName, {'attr': {'class' : 'form-control'}}) }}
              </div>
                <div class="form-group">
                  {{ form_label (form.brandDescription) }}
                  {{ form_widget (form.brandDescription, {'attr': {'class' : 'form-control'}}) }}
                </div>
              <div class="form-group text-center mt-3">
                  <button class="btn btn-primary btn-lg">Update</button>
              </div>
          {{ form_end (form)}}
  </div>   

{% endblock %}