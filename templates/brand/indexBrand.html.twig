{# {% extends 'base.html.twig' %}

{% block title %}Hello BrandController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/dady/xampp/htdocs/laptop2/src/Controller/BrandController.php'|file_link(0) }}">src/Controller/BrandController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'D:/dady/xampp/htdocs/laptop2/templates/brand/index.html.twig'|file_link(0) }}">templates/brand/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %} #}


{% extends 'base.html.twig' %}
{% block title %}Brand List{% endblock %}
{% block body %}
<div class="container-fluid">
<a class="btn btn-primary" href="{{ path ('createBrand') }}">Create New Brand</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">Brand Name</th>
            <th scope="col">Description</th>
            <th scope="col">Functions</th>
        </tr>
    </thead>
    <tbody>
    {% for brand in Brands %}
        <tr>
            <td scope="row">{{ brand.brandName }}</td>
            <td scope="row">{{ brand.brandDescription }}</td>
           
            <td scope="row"> 
                <a class="btn btn-info" href="{{ path ( 'detailBrand' , {'id' : brand.id } ) }}">Detail</a>
                <a class="btn btn-warning" href="{{ path ( 'updateBrand' , {'id' : brand.id } ) }}">Update</a>
                <a class="btn btn-danger" href="{{ path ( 'deleteBrand' , {'id' : brand.id } ) }}"
                                          onclick = "return confirm('Are you sure to delete ?')"
                >Delete</a>
            </td>
        </tr> 
    {% endfor %}
    </tbody>
</table>
</div>
{% endblock %}

